DATABASE_URL=jdbc:postgresql://localhost:5432/gym
DATABASE_USER=gym
DATABASE_PASSWORD=local-development-only
TOKEN_PEPPER=replace-with-at-least-32-random-bytes
GOOGLE_CLIENT_ID=193759830496-lucic3av1r1l4441ed54ap9e30fkolfl.apps.googleusercontent.com
WEB_ORIGIN=https://api.valerochkagym.tech
MAIL_ENABLED=true
MAIL_FROM=noreply@valerochkagym.tech
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_AUTH=false
SMTP_TLS=false
SMTP_SSL=false

# AI provider, API key and models are configured in /admin/ and stored in PostgreSQL.
# Stable AES-256 key: generate with openssl rand -base64 32.
# Production: GitHub Environment production secret, delivered to server by CD.
AI_SETTINGS_ENCRYPTION_KEY=
