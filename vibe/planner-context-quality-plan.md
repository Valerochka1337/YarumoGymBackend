# Контекст Calendar planner v2

База: origin/main 285f222, отдельная feat/planner-context-quality. Android без изменений.

1. Расширить bounded capture: окно 28 локальных дат (64 родителей / 8192 факта),
   до трёх последних завершённых родителей до окна, общий byte guard сохранить.
   Старые факты не участвуют в месячном объёме и назначении веса.
2. Передать последние 3 тренировки, группируя одинаковые результаты подходов без потери
   количества. Остаток окна — непересекающиеся недельные сводки упражнений и мышц.
   Отдельные ссылки кандидатов на последние два наблюдения; никаких вычисленных PR,
   восстановления и сравнения исторического оборудования, которого контракт не хранит.
3. Отбор после существующих hard filters: знакомые упражнения, по две альтернативы
   на мышцу/тип и оборудование/тип; дополнение до 36, расширение без жёсткого top-N.
   При свободных предпочтениях сохранить весь набор: текст не разбирается эвристикой.
4. Версионируемая инструкция; удалить per-candidate coverage/available, агрегаты однократно.
   Бюджет UTF-8 256 KiB с явной ошибкой, не обрезанием ограничений.
5. Синтетические тесты capture/selection/summary, сравнение bytes с baseline; check bootJar.

Контракт ответа заморожен и не содержит rationale/evidence. Не расширять UI/протокол и
не маскировать свободное объяснение под проверенный факт. Rubric AI-eval: соблюдение
ограничений, преемственность, опора на даты/результаты, честность неизвестного, разумность
изменений. Реальный AI-eval требует отдельного разрешения; тесты не доказывают качество плана.
